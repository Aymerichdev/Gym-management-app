# Copia este archivo a .env y ajusta valores para tu entorno local o producción.
# Las variables vacías usan los defaults definidos en settings.py.

# Seguridad y Django
DJANGO_SECRET_KEY=usa-una-clave-segura-en-produccion
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost 127.0.0.1

# Base de datos (por defecto SQLite si se dejan vacíos ENGINE/NAME)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=gym
DB_USER=gym_user
DB_PASSWORD=superseguro
DB_HOST=localhost
DB_PORT=5432
